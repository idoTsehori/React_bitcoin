const INITIAL_STATE = {
  user: {
    name: 'Yoyo',
    coins: 100,
    moves: [],
  },
}

export function userreducer(state = INITIAL_STATE, action = {}) {
  switch (action.type) {
    case value:
      break

    default:
      break
  }
}
